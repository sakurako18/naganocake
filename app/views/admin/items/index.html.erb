  <h3>商品一覧</h3>

    <th>商品id</th>
    <th>商品名</th></th>
    <th>税抜価格</th>
    <th>ジャンル</th>
    <th>ステータス</th>

  <% @items.each do |item| %>

    <td><%= item.1d %></td>
    <%= link_to admin_items_show_path(@item.id) do %>
    <td><%= item.name %></td>
     <% end %>

    <td><%= item.price %></td>
    <td><%= item.genre_id %></td>
    <td><%= item.is_active %></td>

  <%end%>