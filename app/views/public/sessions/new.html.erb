<div class="container">
  <div class="row">
      <div class="col-xs-9 col-xs-offset-1">

<h3>ログイン</h3>

  <%= form_with model: @customer, url: customer_session_path, method: :post, local: true do |f| %>
  <table class=table-borderess>

      <tr>
        <td><%= f.label :"メールアドレス" %></td>
        <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
      </tr>
      <tr>
        <td><%= f.label :"パスワード" %></td>
        <td><%= f.password_field :password, autocomplete: "password" %></td>
      </tr>
  </table>


  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-primary" %>
  </div>
<% end %>

<p>会員登録がお済みでない方</p>
<p><%= link_to "こちら", new_customer_registration_path %>  から新規登録を行ってください。</p>

